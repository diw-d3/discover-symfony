{% extends 'base.html.twig' %}

{% block title %}Contact - {{ parent() }}{% endblock %}

{% block body %}
    <div class="container mb-5">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button formnovalidate class="btn btn-primary">Envoyer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
