{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mb-5">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button formnovalidate class="btn btn-primary">Créer le produit</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
