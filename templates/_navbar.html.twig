<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="#">Shop</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#menu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="menu">
            {% set current_route = app.request.get('_route') %}

            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="#">Accueil</a>
                </li>
                <li class="nav-item {{ current_route == 'product_list' ? 'active' }}">
                    <a class="nav-link" href="{{ path('product_list') }}">Produits</a>
                </li>
                <li class="nav-item {{ current_route == 'product_random' ? 'active' }}">
                    <a class="nav-link" href="{{ path('product_random') }}">Aléatoire</a>
                </li>
                <li class="nav-item {{ current_route == 'hello' ? 'active' }}">
                    <a class="nav-link" href="{{ path('hello') }}">Hello</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
