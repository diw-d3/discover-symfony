<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="{{ path('home') }}">Demo</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#menu">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="menu">
            {% set current_route = app.request.get('_route') %}

            <ul class="navbar-nav mr-auto">
                <li class="nav-item {{ current_route == 'home' ? 'active' }}">
                    <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                </li>
                <li class="nav-item {{ current_route == 'product_list' ? 'active' }}">
                    <a class="nav-link" href="{{ path('product_list') }}">Produits</a>
                </li>
                <li class="nav-item {{ current_route == 'product_random' ? 'active' }}">
                    <a class="nav-link" href="{{ path('product_random') }}">Aléatoire</a>
                </li>
                <li class="nav-item {{ current_route == 'hello' ? 'active' }}">
                    <a class="nav-link" href="{{ path('hello') }}">Hello</a>
                </li>
                <li class="nav-item {{ current_route == 'post' ? 'active' }}">
                    <a class="nav-link" href="{{ path('post') }}">Markdown</a>
                </li>
                <li class="nav-item {{ current_route == 'product_create' ? 'active' }}">
                    <a class="nav-link" href="{{ path('product_create') }}">Créer un produit</a>
                </li>
                <li class="nav-item {{ current_route == 'contact' ? 'active' }}">
                    <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home', {_locale: 'fr'}) }}">FR</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('home', {_locale: 'en'}) }}">EN</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
